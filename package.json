{
  "name": "tailboard",
  "version": "1.0.1",
  "description": "Self-hosted Docker link dashboard with Tailwind palettes",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build:css": "npx tailwindcss -i src/input.css -o src/public/style.css --minify"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
