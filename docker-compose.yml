services:
  tailboard:
    build: .
    container_name: tailboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - tailboard-data:/data
    environment:
      - NODE_ENV=production
      - AUTH_USER=admin
      - AUTH_PASS=123

volumes:
  tailboard-data:
